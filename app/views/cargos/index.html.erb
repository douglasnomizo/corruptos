<div class="page-header"><h1>Lista de cargos</h1></div>

<table class="table table-condensed table-striped">
  <tr>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cargos.each do |cargo| %>
  <tr>
    <td><%= cargo.nome %></td>
    <td><%= link_to content_tag(:span, "Exibir"), cargo, :class => "btn btn-small" %></td>
    <td><%= link_to content_tag(:span, "Alterar"), edit_cargo_path(cargo), :class => "btn btn-small" %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nova Cargo', new_cargo_path %>
