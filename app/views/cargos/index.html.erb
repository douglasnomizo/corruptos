<h1>Lista de cargos</h1>

<table>
  <tr>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cargos.each do |cargo| %>
  <tr>
    <td><%= cargo.nome %></td>
    <td><%= link_to content_tag(:span, "Exibir"), cargo %></td>
    <td><%= link_to content_tag(:span, "Alterar"), edit_cargo_path(cargo) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nova Cargo', new_cargo_path %>
