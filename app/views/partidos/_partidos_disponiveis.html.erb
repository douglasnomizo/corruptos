<% if @partidos %>
  <%= cp.collection_check_boxes :partido_ids, @partidos, :id, :nome, {}, {:class => 'checkbox'} { |input| input.label(class: 'checkbox') { input.check_box + input.text } } %>
<% end %>