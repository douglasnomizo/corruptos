<%= simple_form_for(@candidato) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nome_campanha %>
    <%= f.association :eleitor %>
  </div>

  <div class="form-inputs">
    <%= f.button :submit, "Salvar" %>
  </div>
<% end %>
